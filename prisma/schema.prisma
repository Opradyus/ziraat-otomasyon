// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// --- KULLANICI ROLLERİ ---
enum UserRole {
  ADMIN
  ORGANIZER
  USER
}

// --- KULLANICI VE AUTH MODELLERİ ---
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String    @unique
  emailVerified DateTime?
  image         String?
  password      String?
  
  // Bizim eklediğimiz alanlar
  city          String?
  role          UserRole  @default(USER)
  
  // İlişkiler
  accounts      Account[]
  sessions      Session[]
  events        Event[]
  participations Participant[]
  
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

model Account {
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
 
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
 
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
 
  @@id([provider, providerAccountId])
}

model Session {
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
 
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model VerificationToken {
  identifier String
  token      String
  expires    DateTime
 
  @@id([identifier, token])
}

// --- ETKİNLİK MODELİ ---
model Event {
  id          String   @id @default(cuid())
  title       String
  description String
  
  // --- YENİ EKLENEN ALANLAR ---
  city        String   @default("Belirtilmemiş") 
  district    String   @default("Belirtilmemiş")
  location    String
  
  date        DateTime
  
  userId      String
  createdBy   User     @relation(fields: [userId], references: [id])

  categoryId  String?   
  category    Category? @relation(fields: [categoryId], references: [id])
  
  participants Participant[]
  createdAt    DateTime @default(now())
}

model Participant {
  id        String   @id @default(cuid())
  userId    String
  eventId   String
  joinedAt  DateTime @default(now())

  // İlişkiler
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  event     Event    @relation(fields: [eventId], references: [id], onDelete: Cascade)

  @@unique([userId, eventId])
}

model Category {
  id    String  @id @default(cuid())
  name  String  @unique // Örn: "Tohum Takası", "Hasat Şenliği"
  icon  String? // Örn: "🌱", "🚜" (Görsellik için emoji tutalım)
  
  // İlişki: Bu kategorideki etkinlikler
  events Event[]
}